export default function Card<Any>({title, date}) {
    return (
        <>
            <style jsx>{`
            
            `}</style>
            <div>
                {title}
                {date}
            </div>
        </>
    )
}